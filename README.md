# Task Manager Assignment

Full-stack task manager built with Node.js, TypeScript, Prisma, SQLite, and Next.js.

## Setup

1. Install dependencies with `npm install`
2. Copy `backend/.env.example` to `backend/.env`
3. Copy `frontend/.env.example` to `frontend/.env.local`
4. Run `npm run prisma:migrate`
5. Start the apps with `npm run dev:backend` and `npm run dev:frontend`
   


<img width="1916" height="873" alt="image" src="https://github.com/user-attachments/assets/80777da7-b772-4eac-8d6f-e3e6c225106e" />
